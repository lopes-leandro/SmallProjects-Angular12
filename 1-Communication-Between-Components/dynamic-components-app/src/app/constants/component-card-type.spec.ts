import { ComponentCardType } from './component-card-type';

describe('ComponentCardType', () => {
  it('should create an instance', () => {
    expect(new ComponentCardType()).toBeTruthy();
  });
});
